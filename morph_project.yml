version: '1'

build:
  framework: streamlit
  runtime: python3.12
  package_manager: pip
  
profile: default
project_id:

  # Deployment Settings
deployment:
    provider: gcp
    gcp:
        region: us-central1
        memory: "1Gi"
        cpu: 1
        timeout: 300
  
